name: Build & Push Docker Images

on:
  push:
    branches:
      - main
<<<<<<< HEAD
      - pocketpro-nyl
    tags:
      - 'v*'
  pull_request: # PocketPro:NYL Project
    branches: # PocketPro:NYL Project
      - main # PocketPro:NYL Project
      - pocketpro-nyl # PocketPro:NYL Project
=======
    tags:
      - 'v*'
  pull_request:
    branches:
      - main
>>>>>>> f8d15eea4aab2b9244b47f5221229d448e13a685

env:
  DOCKER_REGISTRY: ghcr.io
  DOCKER_ORG: adportfolionode
  FRONTEND_IMAGE: pocketpro-nyl-frontend
  BACKEND_IMAGE: pocketpro-nyl-backend

jobs:
  build-and-test:
    name: Build & Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Cache Docker layers
        uses: actions/cache@v4
        with:
          path: /tmp/.buildx-cache
          key: ${{ runner.os }}-buildx-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-buildx-

      - name: Build backend image
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          file: ./backend/Dockerfile
          tags: ${{ env.BACKEND_IMAGE }}:test
          load: true
          cache-from: type=local,src=/tmp/.buildx-cache
          cache-to: type=local,dest=/tmp/.buildx-cache-new

      - name: Build frontend image
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          file: ./frontend/Dockerfile
          tags: ${{ env.FRONTEND_IMAGE }}:test
          load: true
          build-args: NODE_VERSION=22.13.1
          cache-from: type=local,src=/tmp/.buildx-cache
          cache-to: type=local,dest=/tmp/.buildx-cache-new

      - name: Test backend image
        run: |
          docker run --rm --user root --entrypoint python ${{ env.BACKEND_IMAGE }}:test -c "import fastapi, hypercorn, chromadb, sklearn; print('All backend imports OK')"

      - name: Test frontend image
        run: |
          docker run --rm ${{ env.FRONTEND_IMAGE }}:test sh -c '[ -f /usr/share/nginx/html/index.html ] && echo "Frontend build OK" || exit 1'

      - name: Move cache
        run: |
          rm -rf /tmp/.buildx-cache
          mv /tmp/.buildx-cache-new /tmp/.buildx-cache

  push-to-registry:
    name: Push to Docker Hub & GHCR
    needs: build-and-test
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Extract metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: |
<<<<<<< HEAD
            ${{ env.DOCKER_REGISTRY }}/${{ env.DOCKER_ORG }}/${{ env.FRONTEND_IMAGE }}
            ${{ env.DOCKER_REGISTRY }}/${{ env.DOCKER_ORG }}/${{ env.BACKEND_IMAGE }}
            ${{ secrets.DOCKER_USERNAME }}/${{ env.FRONTEND_IMAGE }}
            ${{ secrets.DOCKER_USERNAME }}/${{ env.BACKEND_IMAGE }}
=======
            ${{ env.DOCKER_REGISTRY }}/${{ env.DOCKER_ORG }}/pocketpro-nyl-frontend
            ${{ env.DOCKER_REGISTRY }}/${{ env.DOCKER_ORG }}/pocketpro-nyl-backend
            ${{ secrets.DOCKER_USERNAME }}/pocketpro-nyl-frontend
            ${{ secrets.DOCKER_USERNAME }}/pocketpro-nyl-backend
>>>>>>> f8d15eea4aab2b9244b47f5221229d448e13a685
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha
            type=raw,value=latest,enable=${{ github.ref == format('refs/heads/{0}', 'main') }}

      - name: Build and push backend
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          file: ./backend/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push frontend
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          file: ./frontend/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: NODE_VERSION=22.13.1
          cache-from: type=gha
          cache-to: type=gha,mode=max

  release-draft:
    name: Draft Release
    needs: push-to-registry
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          name: Release ${{ github.ref_name }}
          body_path: docs/changes/RELEASE_NOTES.md
          draft: false
          prerelease: false
          generate_release_notes: true
